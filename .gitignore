# Runtime community/demo store — regenerated by server.js on boot
data/results.json
data/*.tmp
data/*.corrupt-*

# Dependencies (project is zero-dependency, but just in case)
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
