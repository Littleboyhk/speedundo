# Runtime community/demo store — regenerated by server.js on boot
data/results.json
data/*.tmp
data/*.corrupt-*

# Dependencies
node_modules/

# Cloudflare Wrangler
.wrangler/
# Local dev secrets / tokens — never commit
.dev.vars
.env

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
